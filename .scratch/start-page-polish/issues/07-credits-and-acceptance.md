# 07: 素材登记与开始页整页验收

**What to build:** 把这次新增的三张图登记进素材来源清单、补上目录说明；并对开始页做一次逐条验收、留下截图证据，同时确认单局页与结算页没有任何回归。这是本次收尾的唯一一票：它交付的是"这次改版可以对外说完成了"这件事。

**Blocked by:** 03、04、05、06

**Status:** done

- [x] 素材来源清单登记三张新图
- [x] README 的目录结构补上界面美术子目录
- [x] 开始页五处元素逐条验收通过（标题艺术字含兜底 / 副标题圆角底框 / 玩法说明新字号颜色位置 / 奖杯 + 最高分含位数变化仍居中 / 木牌按钮与按下反馈）
- [x] 留一张开始页截图作为验收证据（`evidence/start-page-9x16.png`）
- [x] 另留一张比 9:16 更窄的画幅截图（`evidence/start-page-narrow-iphone14pro-393x852.png`）
- [x] 单局页与结算页无回归（结算页最高分仍是旧文案、数值正确；重开一局正常）
- [x] 图片槽位清空时的兜底也验过（标题显兜底文字、按钮显占位底、奖杯不显破图）
- [x] `npm test`（34 例）与类型检查通过；引擎脚本诊断 0 错误
- [x] 交付前再检查一次场景文件差异，确认没有夹带编辑器自动写入的 Canvas 自动布局噪音

## Comments

验收完成（2026-09-15）。本次改动只有两份 Markdown，代码与场景一行没动。

### 改动清单

- `CREDITS.md`：新增「开始页界面图（4 个文件）」一节，登记四张图的规格、实际交付尺寸与已知偏差。
- `README.md`：目录结构补 `resources/art/ui/`；素材来源与授权补界面图一条；文档列表补 `docs/art/ui-prompts.md` 与 ADR-0002。
- 四张图**都已交付**（06 之前随重交付入库），所以清单里没有"待交付"项。

### 验收方式

编辑器窗口抓不到 Game View 画面（03~06 一路卡在这条），因此改用 **浏览器预览 + playwright 驱动**：预览页在 7456 提供服务，可精确控制画幅、可拦截资源请求，验收证据因此可复现。

- 画幅：预览页顶栏的「设计分辨率」下拉切到 Apple iPhone 14 Pro（393×852，FitHeight 下可见宽度约 590 设计像素，正是 spec 说的那档）。
- 兜底：用请求拦截把四张图的 URL 返回 404，**不改文件、不动资源**。
- 按下态：`mousedown` 后直接读场景节点状态（`cc.director.getScene()` 遍历到 `StartButton`），比单看截图更硬。

### 五处元素逐条验收

| # | 元素 | 结论 | 证据 |
| --- | --- | --- | --- |
| ① | 标题艺术字（含兜底） | 艺术字正常；四张图全 404 时退回文字兜底 | `start-page-9x16.png`、`start-page-fallback.png` |
| ② | 副标题圆角底框 | 两种画幅下都完整、不被切 | `start-page-9x16.png`、`start-page-narrow-iphone14pro-393x852.png` |
| ③ | 玩法说明（22 号 / 更淡 / 中间偏下） | 落在背景底部安静区，与副标题分层 | `start-page-9x16.png` |
| ④ | 奖杯 + 最高分（含位数变化仍居中） | 0 与 128 两种位数都整行居中；图缺失时只显示文字 | `start-page-9x16.png`、`start-page-bestscore-128.png` |
| ⑤ | 木牌按钮与按下反馈 | 按下时底图切到 `start-button-pressed`、节点缩到 0.95，"开摊"是子节点跟着一起动 | `button-normal-clip.png` / `button-pressed-clip.png` |

### 无回归 / 兜底 / 窄屏

- **单局页**：点「开摊」正常进入，倒计时、分数、订单卡、碗、配料盘全部照旧（`round-page.png`）。
- **结算页**：跑满 60 秒后那行最高分仍是 **"最高分 0"**（旧文案、无全角冒号），数值正确；点「再来一碗」正常重开一局（`result-page.png`、`restart-round.png`）。
- **兜底**：四张图的请求全返回 404 后，标题显示文字兜底、按钮显示木色圆角占位底、奖杯收起不显破图，四条告警与实现一致，页面不开天窗（`start-page-fallback.png`）。
- **窄屏**：iPhone 14 Pro（393×852）下标题与副标题底框都不被切；另有一张 620×1400 窗口截图，证明桌面浏览器窗口比设计分辨率窄、画布被裁边时两侧内容仍完整（`start-page-narrow-iphone14pro-393x852.png`、`start-page-narrow-620x1400.png`）。

### 命令与诊断结果

- `npm test`：**34 个用例全绿**（核心层 21 + 开始页纯函数 13，本次没有新增接缝，作为回归基线）。
- `npx tsc --noEmit`：**0 错误**。
- 引擎脚本诊断（MCP `get_script_diagnostic_context`）：`exitCode 0`、**0 错误**。
- 场景差异：**本次工作区里 `assets/scenes/main.scene` 没有任何改动**；本特性分支相对 `master` 的场景差异里搜不到 Canvas 自动布局噪音（`1840` 之类的回写值 0 命中）。

### 备注

- **四张图都已交付**，所以"待交付"这一项在清单里没有落地对象；素材侧的规格偏差（标题 2400×800、按钮 2400×800/807 且无中段、奖杯 2048×2048 只占 68%×58%）已在 `CREDITS.md` 汇总登记，重出图同名覆盖即生效。
- 本次不提交两个编辑器自动写入的文件（`funplay-cocos-mcp.config.json` 的 MCP 端口、`settings/v2/packages/information.json` 的 splash 表单 sid），与前几票一致。

### 评审结论

本次交付只有两份文档，无代码 / 场景 / 资源改动。

- **Standards 轴**：文档中文、命名与既有文档一致；`docs/art/ui-prompts.md` 里的交付路径与文件名与 `assets/resources/art/ui/` 实际一致；`CREDITS.md` 的登记格式沿用背景图那一节。
- **Spec 轴**：票面九条 checkbox 逐条对上有据；素材偏差如实登记而非掩盖；未交付项已说明为"无对象"。

