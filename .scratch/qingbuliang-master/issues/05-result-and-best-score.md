# 05: 结算与最高分

**What to build:** 结算页显示本局分数、完成订单数、最高连击；"再来一碗"重开一局；最高分写进本地存储，刷新浏览器后在开始页仍能看到。

**Blocked by:** 04

**Status:** done

- [x] 结算页三项数值与当局实际相符
- [x] 重开后分数、连击、倒计时、订单全部复位
- [x] 刷新浏览器后最高分仍在
- [x] 结算页数值全部来自核心事件，界面不自己算分

## Comments

- 结算页三项直接取核心 `finished` 事件；最高分只做"读出来、比一比、写回去"（`game/bestScore.ts`，读写都兜异常，写盘失败时以内存值为准）。
- 该页是被 `finished` 事件点亮的，其组件在激活时才开始 `onLoad`，订阅不到这条事件，所以由组合根 `GameRoot` 在切页后转交（实现注释已写明）。
- "刷新浏览器后最高分仍在"是真验的：连起两次预览，第二次开局读到 `页面=最高分 135 存储=135`。
- 破纪录提示（"老板娘都服了"）与素材致谢归 11 切片；`recordScore` 目前只返回最高分数值，11 若需要判断"本局是否破纪录"，在那里改成返回 `{ best, improved }`。
