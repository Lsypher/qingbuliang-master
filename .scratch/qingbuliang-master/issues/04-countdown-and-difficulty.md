# 04: 计时与难度换档

**What to build:** 顶部倒计时跑起来（数字 + 进度条），60 秒走完自动进结算页；已用时间越过 20 秒与 40 秒时，新订单的小料数量从 3 变 4、再变 5；出餐过渡的 0.4 秒里倒计时停住不跳。

**Blocked by:** 03

**Status:** done

- [x] 倒计时从 60 秒递减到 0，数值与进度条一致
- [x] 时间归零自动进入结算页，且此后点击配料无反应
- [x] 20 / 40 秒后新订单分别要 4 种、5 种小料
- [x] 出餐换人的 0.4 秒内倒计时数值不变

## Comments

- 核心侧的行为（换档、过渡期暂停、归零结束）由 `tests/session.test.ts` 的 21 个用例守着；本切片补的是表现层接线。
- 运行期验证走 `assets/scripts/debug/SelfCheck.ts`（Game View 自检，36 条断言全绿）。"数值与进度条一致"在 t≈0 / 2 秒 / 48 秒三处取样；排版用节点包围盒断言（Game View 内容区截不到图）。
- 临时按钮 `EndRoundButton` 已按注释约定随本切片从场景删除。
